import '../../domain/repositories/i_{{ snake_name }}_repository.dart';

/// Use case to restore a soft-deleted {{ pascal_case }}
class Restore{{ pascal_case }}UseCase {
  final I{{ pascal_case }}Repository _repository;

  Restore{{ pascal_case }}UseCase({required I{{ pascal_case }}Repository repository})
      : _repository = repository;

  /// Execute the use case
  Future<void> execute(String id) {
    return _repository.restore(id);
  }
}